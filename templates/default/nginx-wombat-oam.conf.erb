upstream wombat_oam {
  server 127.0.0.1:<%= node[:wombat_oam][:wo_rest][:port] %>;
}

server {
  listen 80;
  server_name <%= node[:wombat_oam][:host] %>;

  location / {
    proxy_pass http://wombat_oam;
    proxy_set_header Host $host;
  }
}
